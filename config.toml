[solution]
name = "TheRealDoers_V1"      # Solution name
definition = "sparse_attention"          # Track: fused_moe | sparse_attention | gated_delta_net
author = "TheRealDoers"              # Team/author name

[build]
language = "triton"               # triton | cuda
entry_point = "kernel.py::run_sparse_attention_pipeline"  # Kernel function name
